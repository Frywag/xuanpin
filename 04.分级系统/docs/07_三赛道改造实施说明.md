# 三赛道改造实施说明（tracks.v1，校准态）

> 依据：《开发部大项目7.13_选品项目全景理解与系统定位》《三赛道独立SAB分级_目标业务规则》
> 《选品分级系统P0改进交接与验收清单》（均在 `01.分级标准参考/`）。
> 范围排除（业务确认）：商品立项与产品定义、产品组合与上市回流不在本项目内。

## 1. 已实施（交接清单 §17「可立即实施范围」）

| 项 | 实现 |
|---|---|
| 三结果模型 | `track_eval.py`：每候选每 run 恰好 3 条 TrackEvaluation（唯一键 run_id+subject_type+subject_id+track_id），本轮实测 2,234×3=6,702 条 |
| 状态语义 | ELIGIBLE / PENDING_DATA / REJECTED；PENDING/REJECTED 的正式 grade 为空，仅导出兼容附 legacy_grade=C |
| 赛道键 | trend_new / hit_improvement / long_tail_direct + recommended_development_mode（开发方式与赛道分离，人审确认）；balanced 不再是业务赛道 |
| 趋势新品 | 准入=近期推出证据 AND 款型新颖度证据；**不设任何数据门槛**（销量/评论/GMV/收藏/增长率/畅销榜）；无销量绝不淘汰；多平台相似/内容只加权；单源早期可进 B |
| 爆款改款 | 三项联合准入（适量销量[组内相对分位草案]+低评分[相对/绝对+最小样本]+负面聚类≥2）；缺评论全文→PENDING_DATA；评分正常→REJECTED |
| 长尾直接选品 | 稳定需求（交易类证据，热度不计入；排除单次暴涨）+ 无阻断性质量问题 + 价格可判；不是关键词赛道、不是兜底赛道 |
| 新鲜度契约 | packet.freshness（listed_at/first_seen_at/new_arrival_flag/freshness_status/novelty_status/双证据链）；红线落地：观察≠上架、页面新≠款型新、Shein「新品集合位置」按规则 7.3.A 记为 inferred 证据 |
| 来源角色 | source_roles 多值（sources_p0.yaml 配置：独立站=discovery、Shein=transaction+voc+discovery、插件=transaction[+content]），独立站按发现源参与趋势判断，不再被结构性淘汰（本轮为 PENDING_DATA 待补而非 REJECTED/C） |
| 信号注册表 | track_eval.SIGNAL_REGISTRY 统一交易/热度/VOC 口径（评论数计入交易类）；旧 DEMAND_SIGNAL_FIELDS 标注 LEGACY |
| 机会簇骨架 | 风格词召回（召回≠确认）、cluster_id/version/成员关系证据/lineage、confidence=low 100% human_review=pending |
| 赛道独立预算 | 每赛道独立 l2/l3 队列（草案名额），互不挤占 |
| 交付表 | `三赛道选品推荐表.xlsx`：三张 SAB 表、跨赛道汇总、待补数据、未准入与淘汰依据、机会簇、数据源覆盖与角色、规则版本与运行记录（评分明细/证据索引/152键在主工作簿） |
| 存储 | selection.db 新增 track_evaluations 表；旧 results 表保留为 legacy 兼容 |
| 测试 | tests/test_track_eval.py 覆盖验收场景一/四/五/六/七/八、三条记录不变式、状态语义、确定性重算 |

## 2. 本轮真实数据运行结果（run_mode=full，普通全量运行）

> 口径更正（P0-07，2026-07-14）：run_20260713_001 是**普通全量运行**（当前仓库
> 数据可得性下的完整管道），不是「2,000 款严格快照回放」——严格回放需要
> 06.分级系统数据源 的不可变基线目录，该目录不在本仓库。自本版起 run_meta
> 强制记录 `run_mode`（full / strict_snapshot / enhanced_replay），汇报口径
> 必须与之一致。

run_20260713_001（2,234 候选，不补字段）：

| 赛道 | ELIGIBLE | PENDING_DATA | REJECTED |
|---|---:|---:|---:|
| 趋势新品 | 0 | 2,234 | 0 |
| 爆款改款 | 0 | 2,140 | 94 |
| 长尾直接选品 | 655（草案分 S=29/A=624/B=2） | 1,422 | 157 |

解读：这正是规则文档要求的诚实结果——当前数据没有款型新颖度证据（历史款型库/
更早快照/业务确认均缺失），趋势赛道全部进入待补池并产出补采任务，而不是伪造
趋势等级；改款赛道被评论全文卡住；长尾赛道的草案分数明显未校准（A 偏多），
`rule_status=calibration` 如实标注。**受控增强回放**需在补采（上架时间、评论
全文、图片相似）后进行。7.11 的 2,000 款基线（06.分级系统数据源）不在本仓库，
回放对照需在其所在环境执行。

## 2.5 业务确认追加（2026-07-13）

**趋势新品赛道与评论等数据点位彻底解耦**：rating/review_count/销量/GMV/收藏/
增长率列入趋势赛道 forbidden_signals（tracks_v1.yaml），准入与评分一律不得读取——
独立站等发现源与交易平台公平竞争趋势 S/A/B（公平性有自动化测试保证：同一候选
带不带评论/销量，趋势结果必须完全一致）。**评论全量采集与分析后置为 L3 终选
验证事项**（趋势赛道 l2_focus 不含评论，l3_focus 含之；评论作为核心准入证据
仅属于爆款改款赛道）。

## 2.6 执行矛盾修订（2026-07-14，依据《选品项目当前执行矛盾与流程待确认问题》）

按该文档 §7 临时执行规则，本仓库落地了以下**结构性修订**（不替业务拍板）：

| 项 | 修订 |
|---|---|
| P0-01 双链语义 | 过渡期新旧双链并行、双口径标注：legacy=基线参考（非正式生产推荐），tracks.v1=校准态（非正式生产等级）。run_meta 增 `result_semantics` 全文声明；legacy 选品推荐表「运行记录」sheet 首行加口径免责；唯一事实源切换时点仍待业务确认 |
| P0-03 补采死锁 | 预算两类拆分：`refetch_queue`（准入补证预算，从 PENDING_DATA 取，条目带 missing/tasks/status=open/owner/due 占位）与 l2/l3 深挖预算（仅 ELIGIBLE）互不挤占；tracks_v1.yaml 每赛道新增 `refetch_budget_draft`（20/20/15，草案）；三赛道表「待补数据」sheet 标注每行 入队(open)/待下轮 |
| P0-04 跨赛道决策 | 「跨赛道汇总」删除按最高分自动带出开发方式的口径——三赛道分数未经金标统一标尺，禁止跨赛道直接比较；改为并列展示全部已准入赛道的开发方式，最终选择人工决策 |
| P0-07 回放可审计 | run_meta 增 `run_mode`+`run_mode_semantics`；CLI `run --run-mode {full,strict_snapshot,enhanced_replay}`；run_meta 中配置/DB 路径改为仓库相对路径；缺失数据源工作簿快速失败并给出明确文件清单（不再在 openpyxl 深处抛裸异常）；E2E 测试对缺失工作簿明确跳过并说明原因 |
| P0-08 查询口径 | CLI `grades` 默认双口径输出（tracks_v1_calibration + legacy_baseline，各带 note），`--legacy` 兼容旧脚本；MCP `query_grades` 同步双口径；`run` 命令输出逐行标注口径 |
| P0-09 状态机 | tracks_v1.yaml 增 `project_status: FRAMEWORK_READY_PARTIAL_CALIBRATION`（FRAMEWORK_READY → CALIBRATION_READY → PRODUCTION_ENABLED → LISTING_READY），写入 run_meta 与三赛道表「规则版本与运行记录」sheet |

**明确不由 agent 决定、只登记进 tracks_v1.yaml `pending_business_confirmation`
的业务拍板项**（该文档 §9 边界）：唯一业务事实源切换时点（P0-01）、三赛道编排是否
接管深评/152/LLM 对象与真实采集成本投向（P0-02/P0-05）、补采任务负责人/到期/回灌
run 语义（P0-03 运营侧）、跨赛道最终决策机制（P0-04）、趋势赛道预算对象簇 vs 单品
（P0-06）、2,000 款不可变基线目录清单（P0-07）。

## 2.7 临时可跑占位规则（2026-07-14 业务指示）

业务确认：选品阶段大量缺失是**数据源结构性没有**（站点/源本来就不含该点位），
不是漏采。据此，缺证据的候选不再卡在待补池外面，而是按**占位规则临时准入、
照常评分进入筛选流程**；缺失照记 missing_fields、补证照排 refetch_queue。
规则集中在 tracks_v1.yaml 各赛道 `provisional_admission` 段（status=
interim_runnable_defaults），**人工确定最终门限/字段后改配置即生效**，
`enabled: false` 即回到严格证据口径。

| 赛道 | 占位规则 | 不豁免的底线 |
|---|---|---|
| 趋势新品 | 无「旧款确证」即临时准入；无新鲜度证据按 0.3 占位系数计分（confirmed=1.0/inferred=0.7），等级仍由证据强度决定，不设上限 | 确证旧款仍 REJECTED；禁读评论/销量等信号（公平性测试覆盖占位路径） |
| 爆款改款 | 有交易信号 + 确证低评分（样本≥30）时，负面聚类缺失（源无评论全文）占位豁免准入 | 占位等级上限 B（痛点未经聚类确认，S/A 须待评论回补）；评分缺失/样本不足仍 PENDING |
| 长尾直接 | 仅缺价格时占位准入 | 占位等级上限 B（价格/利润不可判）；无交易证据仍 PENDING（稳定需求是前提）；单次暴涨排除不豁免 |

所有临时准入记录带 `admission_basis=provisional_rule` + provisional_notes +
confidence=low，SAB 表增「准入依据」列（确证/临时规则(占位)），「待补数据」
sheet 标注 临时准入/待补准入。补证预算改为：临时准入的高分候选优先
（补证后可确证/解除上限），其后是 PENDING 候选；同一候选可同时占深挖与
补证名额。占位规则的最终门限与字段已登记 pending_business_confirmation。

## 3. 待业务确认（阻塞正式生产等级，清单见 tracks_v1.yaml）

新款证据有效期、各来源新品标签可信度、适量销量分档、低评分边界与最小样本、
负面聚类口径、三赛道 S/A/B 阈值与 L2/L3 名额、款型历史库与回看窗口、
same_style/same_trend 阈值、确定性 REJECTED 红线。金标未确认前，
所有产出带 `rule_status=calibration_pending_business_approval`。

## 4. 旧版兼容（过渡期双链语义）

legacy 单赛道管道（assign_track/score/grade、选品推荐表.xlsx、results 表）
完整保留并继续运行，定位为**基线参考，非正式生产推荐**（工作簿与查询输出
均带口径标注）；新旧结果通过 rule_version 区分，不互相覆盖。深评/152/LLM
任务对象暂仍由 legacy 门控驱动（是否切换到三赛道编排属业务拍板项 P0-02）。
回滚方式：删除 configs/tracks_v1.yaml 即回到纯 legacy 行为
（此时 run_meta.project_status 记 legacy_only）。
